---
export interface Props {
  href: string;
  class?: string;
}

const { href, class: className } = Astro.props;
---

<a 
  href={href} 
  class:list={[
    "inline-block font-medium rounded-lg bg-primary text-white hover:bg-primary/90 transition-colors",
    className
  ]}
>
  <slot />
</a> 