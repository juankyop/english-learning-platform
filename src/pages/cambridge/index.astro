---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { b2FirstExam } from '../../data/cambridge/b2-first';

const exams = [
  {
    ...b2FirstExam,
    description: 'First Certificate in English - El examen más popular de nivel intermedio alto',
    icon: '/icons/b2-icon.svg',
    sections: [
      {
        name: 'Reading & Use of English',
        description: 'Comprensión lectora y uso del inglés',
        duration: '1 hora 15 minutos',
        parts: '7 partes',
        href: '/cambridge/b2-first'
      },
      {
        name: 'Writing',
        description: 'Expresión escrita',
        duration: '1 hora 20 minutos',
        parts: '2 partes',
        href: '/cambridge/b2-first/writing'
      },
      {
        name: 'Listening',
        description: 'Comprensión auditiva',
        duration: '40 minutos',
        parts: '4 partes',
        href: '/cambridge/b2-first/listening'
      },
      {
        name: 'Speaking',
        description: 'Expresión oral',
        duration: '14 minutos',
        parts: '4 partes',
        href: '/cambridge/b2-first/speaking'
      }
    ]
  }
  // Aquí puedes añadir más exámenes (C1, C2, etc.)
];
---

<BaseLayout title="Cambridge Exams">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <header class="mb-12">
      <h1 class="text-4xl font-bold text-gray-900 mb-4">Cambridge English Exams</h1>
      <p class="text-xl text-gray-600">
        Prepárate para los exámenes oficiales de Cambridge con nuestros recursos y guías detalladas
      </p>
    </header>

    <div class="space-y-12">
      {exams.map(exam => (
        <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
          <div class="p-8">
            <div class="flex items-start gap-6">
              <div class="w-16 h-16">
                <img src={exam.icon} alt={exam.name} class="w-full h-full" />
              </div>
              <div>
                <div class="flex items-center gap-4 mb-2">
                  <h2 class="text-2xl font-bold text-gray-900">{exam.name}</h2>
                  <span class="px-3 py-1 text-sm font-semibold text-primary bg-primary/10 rounded-full">
                    {exam.level}
                  </span>
                </div>
                <p class="text-gray-600">{exam.description}</p>
              </div>
            </div>
          </div>

          <div class="grid sm:grid-cols-2 lg:grid-cols-4 divide-x divide-y sm:divide-y-0 divide-gray-100">
            {exam.sections.map(section => (
              <a 
                href={section.href}
                class="p-6 hover:bg-gray-50 transition-colors group"
              >
                <div class="flex flex-col h-full">
                  <h3 class="text-lg font-semibold text-gray-900 group-hover:text-primary mb-2">
                    {section.name}
                  </h3>
                  <p class="text-gray-600 mb-4 flex-grow">
                    {section.description}
                  </p>
                  <div class="flex items-center justify-between text-sm text-gray-500">
                    <span>{section.duration}</span>
                    <span>{section.parts}</span>
                  </div>
                </div>
              </a>
            ))}
          </div>
        </div>
      ))}
    </div>

    <div class="mt-12 bg-blue-50 rounded-xl p-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-4">¿Por qué preparar un examen de Cambridge?</h2>
      <div class="grid md:grid-cols-3 gap-6">
        <div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Reconocimiento Internacional</h3>
          <p class="text-gray-600">
            Los certificados de Cambridge son reconocidos por más de 25,000 instituciones en todo el mundo.
          </p>
        </div>
        <div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Validez Permanente</h3>
          <p class="text-gray-600">
            Tu certificado no caduca, manteniendo su validez durante toda tu vida.
          </p>
        </div>
        <div>
          <h3 class="text-lg font-semibold text-gray-900 mb-2">Desarrollo Profesional</h3>
          <p class="text-gray-600">
            Mejora tus oportunidades laborales y académicas con una certificación oficial.
          </p>
        </div>
      </div>
    </div>
  </div>
</BaseLayout> 