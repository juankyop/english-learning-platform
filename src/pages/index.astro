---
import BaseLayout from '../layouts/BaseLayout.astro';
import Button from '../components/ui/Button.astro';

const features = [
	{
		icon: '🎯',
		title: 'Teoría estructurada',
		description: 'Contenido organizado por niveles y temas gramaticales, con explicaciones claras y ejemplos prácticos.'
	},
	{
		icon: '✍️',
		title: 'Ejercicios prácticos',
		description: 'Practica lo aprendido con ejercicios interactivos y recibe feedback instantáneo.'
	},
	{
		icon: '📝',
		title: 'Writing Skills',
		description: 'Mejora tu escritura con modelos, ejemplos y correcciones personalizadas.'
	}
];

const levels = ['A1', 'A2', 'B1', 'B2', 'C1', 'C2'];
---

<BaseLayout title="Bienvenido a English Learning">
	<!-- Hero Section -->
	<section class="relative overflow-hidden">
		<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20 md:py-28">
			<div class="text-center relative z-10">
				<h1 class="text-4xl md:text-6xl font-bold text-gray-900 mb-6">
					<span class="block">Aprende inglés de manera</span>
					<span class="block text-primary">efectiva y estructurada</span>
				</h1>
				<p class="mt-3 max-w-2xl mx-auto text-lg md:text-xl text-gray-600">
					Domina la gramática, mejora tu writing y practica con ejercicios interactivos.
				</p>
				<div class="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
					<Button href="/documentation" class="px-8 py-4 text-lg">
						Comenzar a aprender
					</Button>
					<Button href="/exercises" class="px-8 py-4 text-lg bg-gray-100 text-gray-800 hover:bg-gray-200">
						Probar ejercicios
					</Button>
				</div>
			</div>
		</div>
		<!-- Decorative background elements -->
		<div class="absolute top-0 left-1/2 -translate-x-1/2 w-[120%] h-[120%] -z-10">
			<div class="absolute inset-0 bg-gradient-to-b from-primary/5 to-transparent"></div>
			<div class="absolute top-1/4 left-1/4 w-64 h-64 bg-primary/5 rounded-full blur-3xl"></div>
			<div class="absolute top-1/3 right-1/4 w-48 h-48 bg-secondary/5 rounded-full blur-2xl"></div>
		</div>
	</section>

	<!-- Features Section -->
	<section class="py-20 bg-gray-50">
		<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
			<h2 class="text-3xl font-bold text-center text-gray-900 mb-12">
				Todo lo que necesitas para dominar el inglés
			</h2>
			<div class="grid grid-cols-1 md:grid-cols-3 gap-8">
				{features.map(feature => (
					<div class="bg-white p-8 rounded-2xl shadow-sm hover:shadow-md transition-shadow">
						<div class="text-4xl mb-4">{feature.icon}</div>
						<h3 class="text-xl font-semibold text-gray-900 mb-3">{feature.title}</h3>
						<p class="text-gray-600">{feature.description}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<!-- Levels Section -->
	<section class="py-20">
		<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
			<h2 class="text-3xl font-bold text-center text-gray-900 mb-12">
				Niveles disponibles
			</h2>
			<div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-6 gap-4">
				{levels.map(level => (
					<div class="bg-white border border-gray-200 rounded-xl p-6 text-center hover:border-primary transition-colors">
						<span class="text-2xl font-bold text-primary">{level}</span>
					</div>
				))}
			</div>
		</div>
	</section>

	<!-- CTA Section -->
	<section class="py-20 bg-primary text-white">
		<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
			<h2 class="text-3xl font-bold mb-6">¿Listo para empezar?</h2>
			<p class="text-xl mb-8 text-white/90">
				Únete a nuestra plataforma y comienza tu viaje hacia el dominio del inglés.
			</p>
			<Button href="/documentation" class="px-8 py-4 text-lg bg-white text-primary hover:bg-white/90">
				Comenzar ahora
			</Button>
		</div>
	</section>
</BaseLayout>

<style>
	.hover\:shadow-md {
		transition: box-shadow 0.3s ease;
	}
</style>
